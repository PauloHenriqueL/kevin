# ── Python ──
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
*.egg

# ── Virtualenv ──
venv/
.venv/
env/

# ── Django ──
db.sqlite3
db.sqlite3-journal
staticfiles/          # gerado por collectstatic
media/                # uploads em runtime
*.log

# ── Ambiente e segredos ──
.env
.env.*
!.env.example         # o exemplo (sem segredos) FICA versionado

# ── IDE / editor ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── SO ──
.DS_Store
Thumbs.db

# ── Claude Code / ferramentas locais ──
.claude/
# CLAUDE.md É versionado de propósito: contém o workflow, as regras de negócio
# e as regras do prompt — o time todo precisa ver.

# ── Testes / cobertura / node (playwright de validação) ──
.coverage
htmlcov/
.pytest_cache/
node_modules/
package-lock.json

# ── Entregas do animador ──
# Os .zip nunca entram no repo. Extraia e mova só os arquivos usados.
export_*.zip
export_*/
# Projeto do animador (centenas de MB — masters .mov, node_modules)
kevin_animador.zip
kevin_animador/
*.mov

# ── Mídia pesada do puppet ──
# Backgrounds e vídeo de transição vão para storage externo (S3/R2), não Git.
# Só código (JS/CSS/SVG) fica versionado. Ver CLAUDE.md → "Assets do Kevin".
static/js/kevin-puppet/assets/backgrounds/
static/js/kevin-puppet/assets/videos/

# ── Material bruto do cliente ──
# docx/pdf/mp3 da Bebelingue — apoio, não faz parte do app.
documento_escola/

# ── Áudios de teste locais ──
audios/
